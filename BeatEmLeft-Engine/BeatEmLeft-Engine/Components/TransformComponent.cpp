#include "TransformComponent.h"

//does not need any logic in these functions
//as this component will only contain the data required to do the game logic
//in various other systems (e.g. renderSystem needs this to convert 
//game world coordinates (floating point) to screen coordinates (integers))
void TransformComponent::Init()
{

}
void TransformComponent::Update(float deltaTime)
{

}
